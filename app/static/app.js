const $=id=>document.getElementById(id);
let token=localStorage.getItem('ad_token')||'';
let state={projects:[],assets:[],jobs:[],trends:[],feedbackCount:0};
let selected=new Set();
let health={};
let refreshTimer=null;
const titles={studio:'Studio de création',pipeline:'Pipeline de production',gallery:'Galerie finale',intelligence:'Content Intelligence',learning:'Performance Memory',publication:'Centre de publication'};
function toast(msg,type='ok'){const t=$('toast');t.textContent=msg;t.className='toast show '+(type==='error'?'error':'');clearTimeout(t._timer);t._timer=setTimeout(()=>t.className='toast',3500)}
function auth(){return {'Authorization':'Bearer '+token}}
async function request(url,opts={}){opts.headers={...(opts.headers||{}),...auth()};const r=await fetch(url,opts);let payload=null;try{payload=await r.json()}catch{payload=await r.text()}if(!r.ok){if(r.status===401){logout();throw Error('Session expirée. Reconnecte-toi.')}throw Error(payload?.detail||payload||`Erreur ${r.status}`)}return payload}
function logout(){localStorage.removeItem('ad_token');token='';$('studio').classList.add('hidden');$('loginView').classList.remove('hidden');clearInterval(refreshTimer);refreshTimer=null}
async function login(){try{$('loginError').textContent='';const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:$('loginPassword').value})});const p=await r.json();if(!r.ok)throw Error(p.detail||'Connexion refusée');token=p.token;localStorage.setItem('ad_token',token);openStudio();await loadAll()}catch(e){$('loginError').textContent=e.message}}
function openStudio(){$('loginView').classList.add('hidden');$('studio').classList.remove('hidden');if(!refreshTimer)refreshTimer=setInterval(()=>loadAll(true),5000)}
async function getHealth(){try{health=await fetch('/health').then(r=>r.json());$('statusDot').className=health.ok?'ok':'';$('serviceLabel').textContent=health.ok?'Services en ligne':'Services dégradés';$('statAi').textContent=health.ai==='openai'?'OpenAI':'Local'}catch{$('serviceLabel').textContent='Hors ligne'}}
async function loadAll(silent=false){try{await getHealth();state=await request('/api/dashboard');renderAll()}catch(e){if(!silent)toast(e.message,'error')}}
function currentProjectId(){return $('projectSelect').value||state.projects[0]?.id||''}
function projectAssets(kind='source'){const pid=currentProjectId();return state.assets.filter(a=>a.projectId===pid&&a.kind===kind)}
function renderAll(){const old=$('projectSelect').value;$('projectSelect').innerHTML='<option value="">Sélectionner un projet…</option>'+state.projects.map(p=>`<option value="${p.id}">${esc(p.name)}</option>`).join('');if(old&&state.projects.some(p=>p.id===old))$('projectSelect').value=old;else if(state.projects.length)$('projectSelect').value=state.projects[0].id;$('statProjects').textContent=state.projects.length;$('statSources').textContent=state.assets.filter(a=>a.kind==='source').length;$('statRenders').textContent=state.assets.filter(a=>a.kind==='render').length;$('navJobs').textContent=state.jobs.filter(j=>['queued','running'].includes(j.status)).length;$('navRenders').textContent=state.assets.filter(a=>a.kind==='render').length;renderSources();renderJobs();renderGallery();renderIntel();renderTrends();renderSelectors()}
function esc(s=''){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
async function createProject(){const name=$('projectName').value.trim();if(!name)return toast('Donne un nom au projet.','error');const b=$('createProjectBtn');b.disabled=true;b.textContent='Création…';try{const p=await request('/api/projects',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name})});$('projectName').value='';await loadAll();$('projectSelect').value=p.id;renderAll();toast(`Projet “${p.name}” créé`)}catch(e){toast(e.message,'error')}finally{b.disabled=false;b.textContent='Créer'}}
function renderSources(){const list=$('sourceList'),items=projectAssets('source');$('projectHint').textContent=state.projects.length?`${items.length} fichier(s) dans le projet actif.`:'Crée un projet pour commencer.';if(!items.length){list.className='source-list empty-state';list.innerHTML='Aucun rush dans ce projet.';selected.clear();updateSelection();return}list.className='source-list';list.innerHTML=items.map(a=>`<div class="source ${selected.has(a.id)?'selected':''}" data-id="${a.id}"><input class="source-check" type="checkbox" ${selected.has(a.id)?'checked':''}><div><strong>${esc(a.name)}</strong><small>${(a.size/1048576).toFixed(1)} Mo ${a.metadata?.medal?'· Medal':''}</small></div><select class="role" data-role="${a.id}"><option value="source" ${a.role==='source'?'selected':''}>Rush</option><option value="reference" ${a.role==='reference'?'selected':''}>Référence</option><option value="broll" ${a.role==='broll'?'selected':''}>B-roll</option><option value="talking_head" ${a.role==='talking_head'?'selected':''}>Facecam</option></select><button class="delete" data-delete="${a.id}" title="Supprimer">×</button></div>`).join('');list.querySelectorAll('.source-check').forEach(x=>x.onchange=e=>{const id=e.target.closest('.source').dataset.id;e.target.checked?selected.add(id):selected.delete(id);renderSources()});list.querySelectorAll('[data-role]').forEach(x=>x.onchange=async e=>{try{await request(`/api/assets/${e.target.dataset.role}/role`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({role:e.target.value})});toast('Rôle mis à jour');await loadAll()}catch(err){toast(err.message,'error')}});list.querySelectorAll('[data-delete]').forEach(x=>x.onclick=async e=>{if(!confirm('Supprimer ce fichier ?'))return;try{await request(`/api/assets/${e.currentTarget.dataset.delete}`,{method:'DELETE'});selected.delete(e.currentTarget.dataset.delete);await loadAll()}catch(err){toast(err.message,'error')}});updateSelection()}
function updateSelection(){const items=projectAssets('source'),refs=items.filter(a=>a.role==='reference').length;$('selectionSummary').textContent=`${selected.size} rush(s) sélectionné(s)`;$('referenceSummary').textContent=`${refs} référence(s)`;$('launchBtn').disabled=!currentProjectId()||!items.length}
async function uploadFiles(files){if(!currentProjectId())return toast('Crée ou sélectionne un projet avant l’import.','error');const videos=[...files].filter(f=>f.type.startsWith('video/')||/\.(mp4|mov|webm|mkv|avi|m4v)$/i.test(f.name));if(!videos.length)return toast('Aucune vidéo détectée.','error');$('uploadQueue').innerHTML='';for(const f of videos){const row=document.createElement('div');row.className='upload-row';row.innerHTML=`<span>${esc(f.name)}</span><b>0%</b><div class="progress"><i style="width:4%"></i></div>`;$('uploadQueue').appendChild(row);try{if(f.size>(health.maxUploadMb||80)*1048576)throw Error(`>${health.maxUploadMb||80} Mo`);row.querySelector('b').textContent='Envoi…';const fd=new FormData();fd.append('file',f);fd.append('project_id',currentProjectId());fd.append('role','source');await request('/api/assets',{method:'POST',body:fd});row.querySelector('b').textContent='OK';row.querySelector('i').style.width='100%'}catch(e){row.querySelector('b').textContent='Erreur';row.style.borderColor='#6b2637';toast(`${f.name}: ${e.message}`,'error')}}await loadAll()}
async function launchJob(){const pid=currentProjectId();if(!pid)return toast('Sélectionne un projet.','error');const all=projectAssets('source').filter(a=>a.role!=='reference');const ids=selected.size?[...selected].filter(id=>all.some(a=>a.id===id)):all.map(a=>a.id);if(!ids.length)return toast('Ajoute au moins un rush source.','error');const b=$('launchBtn');b.disabled=true;b.textContent='Création du job…';try{await request('/api/jobs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({projectId:pid,assetIds:ids,variants:+$('variants').value,captions:$('captions').checked,voiceover:$('voiceover').value,autoRevision:$('autoRevision').checked,targetDuration:+$('targetDuration').value})});toast('Job lancé. Le worker traite la vidéo en arrière-plan.');switchTab('pipeline');await loadAll()}catch(e){toast(e.message,'error')}finally{b.disabled=false;b.textContent='▶ Lancer le bot complet'}}
function renderJobs(){const el=$('jobList');if(!state.jobs.length){el.innerHTML='<div class="empty-state">Aucun job pour le moment.</div>';return}el.innerHTML=state.jobs.map(j=>{const p=state.projects.find(x=>x.id===j.projectId);return `<div class="job-card"><div class="job-top"><div><div class="job-title"><span class="badge ${j.status}">${j.status}</span><b>${esc(p?.name||'Projet')}</b></div><div class="job-meta">${esc(j.stage)} · ${j.variants} variante(s) ${j.strategy?'· '+esc(j.strategy):''}</div></div><div class="job-actions">${['queued','running'].includes(j.status)?`<button class="btn tiny ghost" data-cancel="${j.id}">Annuler</button>`:''}${['failed','cancelled'].includes(j.status)?`<button class="btn tiny secondary" data-retry="${j.id}">Relancer</button>`:''}</div></div><div class="bar"><i style="width:${j.progress||0}%"></i></div><div class="job-meta">${esc(j.message||'')} ${j.criticScore!=null?`· Score <b>${Number(j.criticScore).toFixed(1)}/100</b>`:''}${j.revisionCount?` · Révision ${j.revisionCount}`:''}</div></div>`}).join('');el.querySelectorAll('[data-cancel]').forEach(x=>x.onclick=async()=>{try{await request(`/api/jobs/${x.dataset.cancel}/cancel`,{method:'POST'});await loadAll()}catch(e){toast(e.message,'error')}});el.querySelectorAll('[data-retry]').forEach(x=>x.onclick=async()=>{try{await request(`/api/jobs/${x.dataset.retry}/retry`,{method:'POST'});await loadAll()}catch(e){toast(e.message,'error')}})}
function renderGallery(){const renders=state.assets.filter(a=>a.kind==='render'),el=$('gallery');if(!renders.length){el.innerHTML='<div class="empty-state">Tes rendus apparaîtront ici après le premier job terminé.</div>';return}el.innerHTML=renders.map(a=>{const j=state.jobs.find(j=>(j.outputAssetIds||[]).includes(a.id));const src=`/api/assets/${a.id}/download?token=${encodeURIComponent(token)}`;return `<article class="render-card"><div class="video-shell"><video controls preload="metadata" src="${src}"></video></div><div class="render-info"><h4>${esc(a.name)}</h4><div class="job-meta">${(a.size/1048576).toFixed(1)} Mo ${j?.criticScore!=null?`· <span class="score">${Number(j.criticScore).toFixed(1)}/100</span>`:''}</div><div class="render-actions"><a class="btn tiny ghost" href="${src}" target="_blank">Voir</a><a class="btn tiny secondary" href="${src}" download>Télécharger</a></div></div></article>`}).join('')}
function renderIntel(){const items=projectAssets('source'),refs=items.filter(a=>a.role==='reference').length,medal=items.filter(a=>a.metadata?.medal).length,done=state.jobs.filter(j=>j.projectId===currentProjectId()&&j.status==='done'),avg=done.length?done.reduce((s,j)=>s+(j.criticScore||0),0)/done.length:0;$('intel').innerHTML=`<div class="metric"><b>${items.length}</b><span>Sources détectées</span></div><div class="metric"><b>${refs}</b><span>Références de style</span></div><div class="metric"><b>${medal}</b><span>Clips Medal</span></div><div class="metric"><b>${avg?avg.toFixed(1):'—'}</b><span>Score moyen</span></div>`}
function renderTrends(){$('trendList').innerHTML=state.trends.length?state.trends.map(t=>`<div class="trend"><b>${esc(t.label)}</b><span>${esc(t.notes||t.sourceUrl||'Référence enregistrée')}</span></div>`).join(''):'<div class="empty-state">Aucune tendance enregistrée.</div>'}
async function addTrend(){const label=$('trendLabel').value.trim();if(!label)return toast('Donne un nom à la tendance.','error');try{await request('/api/trends',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({label,sourceUrl:$('trendUrl').value.trim(),notes:$('trendNotes').value.trim()})});$('trendLabel').value=$('trendUrl').value=$('trendNotes').value='';await loadAll();toast('Tendance ajoutée')}catch(e){toast(e.message,'error')}}
function renderSelectors(){const renders=state.assets.filter(a=>a.kind==='render'),opts='<option value="">Sélectionner…</option>'+renders.map(a=>`<option value="${a.id}">${esc(a.name)}</option>`).join('');$('feedbackAsset').innerHTML=opts;$('publicationAsset').innerHTML=opts}
async function loadLearning(){try{const x=await request('/api/learning');$('learningStats').innerHTML=`<div><b>${Number(x.views).toLocaleString('fr-FR')}</b><span>Vues</span></div><div><b>${Number(x.likes).toLocaleString('fr-FR')}</b><span>Likes</span></div><div><b>${Number(x.shares).toLocaleString('fr-FR')}</b><span>Partages</span></div><div><b>${(Number(x.completion||0)*100).toFixed(0)}%</b><span>Completion moyenne</span></div>`}catch(e){toast(e.message,'error')}}
async function saveFeedback(){const id=$('feedbackAsset').value;if(!id)return toast('Choisis un rendu.','error');const body={views:+$('fViews').value||0,likes:+$('fLikes').value||0,shares:+$('fShares').value||0,completion:+$('fCompletion').value||0,conversions:+$('fConversions').value||0};try{await request(`/api/feedback/${id}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});toast('Performances enregistrées');loadLearning()}catch(e){toast(e.message,'error')}}
async function generatePack(){const id=$('publicationAsset').value;if(!id)return toast('Choisis un rendu.','error');try{const p=await request(`/api/publication/${id}`);$('publicationPack').textContent=`CAPTION\n${p.caption}\n\nHASHTAGS\n${p.hashtags.join(' ')}\n\nCTA\n${p.cta}\n\nScore: ${p.score??'—'} / 100\nStratégie: ${p.strategy}`;}catch(e){toast(e.message,'error')}}
function switchTab(name){document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));document.querySelectorAll('#nav button').forEach(x=>x.classList.toggle('active',x.dataset.tab===name));$(`tab-${name}`).classList.add('active');$('pageTitle').textContent=titles[name]||'Studio';if(name==='learning')loadLearning()}
$('loginBtn').onclick=login;$('loginPassword').onkeydown=e=>{if(e.key==='Enter')login()};$('logoutBtn').onclick=logout;$('refreshBtn').onclick=()=>loadAll();$('createProjectBtn').onclick=createProject;$('projectName').onkeydown=e=>{if(e.key==='Enter')createProject()};$('projectSelect').onchange=()=>{selected.clear();renderAll()};$('selectAllBtn').onclick=()=>{projectAssets('source').filter(a=>a.role!=='reference').forEach(a=>selected.add(a.id));renderSources()};$('selectNoneBtn').onclick=()=>{selected.clear();renderSources()};$('launchBtn').onclick=launchJob;$('addTrendBtn').onclick=addTrend;$('saveFeedbackBtn').onclick=saveFeedback;$('generatePackBtn').onclick=generatePack;document.querySelectorAll('#nav button').forEach(x=>x.onclick=()=>switchTab(x.dataset.tab));
const dz=$('dropzone');['dragenter','dragover'].forEach(ev=>dz.addEventListener(ev,e=>{e.preventDefault();dz.classList.add('drag')}));['dragleave','drop'].forEach(ev=>dz.addEventListener(ev,e=>{e.preventDefault();dz.classList.remove('drag')}));dz.addEventListener('drop',e=>uploadFiles(e.dataTransfer.files));$('filePicker').onchange=e=>uploadFiles(e.target.files);$('folderPicker').onchange=e=>uploadFiles(e.target.files);
if(token){openStudio();loadAll()}else $('loginPassword').focus();